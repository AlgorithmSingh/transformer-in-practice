

| 1 | 0:03 If you work with large language models, whether you're building applications, fine-tuning, 0:03 or using them through an API, you're working with transformer models. And a lot of great 0:08 resources have been created to explain how they work. Courses that build them from scratch, 0:14 complex explanations of the attention mechanism, and theoretical deep dives. 0:20 But you might find that those explanations don't always connect to the problems you actually face 0:26 using transformers. This course connects all the critical pieces to building and using transformer 0:33 networks in practice. It builds a coherent picture from how a trained transformer generates text, 0:40 to what's happening during a forward pass of the model, to how the process gets optimized 0:46 to run on GPUs. I'm delighted to introduce Sharon Zhou, who's your instructor for this course on 0:53 transformers in practice. I've known and worked with Sharon for many years, starting from her days at 0:58 Stanford, and we've really stayed friends and kept in touch through her now being VP of Eng and AI at 1:05 AMD. So Sharon, say a bit more about why you built this course. Yeah, absolutely. The goal of this 1:11 course is really to help you, the learner, gain deeper intuition in a way that impacts how you can 1:17 practically use LLMs, not just the fun math part. I've noticed, you know, people often hit on some 1:23 issue when they're working with language models, and if only they had that deeper intuition, 1:28 they could actually debug it instead of say, well, LLMs don't work. I think that once you 1:33 understand the core of what these models do, generating text one token at a time from a 1:39 probability distribution, everything else follows. Now, one interesting evolution of the transformer 1:45 network is the original Google Brain paper on the transformer network talked about the encoder-decoder 1:52 architectures, and one of the most important evolutions has been that all the frontier labs now 1:57 pretty much use the decoder-only architecture. Yeah, in this course, the focus is really on 2:02 decoder-only transformers, because that's the architecture behind nearly all the frontier 2:06 models that you're using today. So you'll go deep on practical concepts that matter most for your 2:11 work. And throughout this course, there are some also really fun, informative, interactive 2:16 visualizations where you can actually see these processes in action. Like you can watch tokens get 2:21 sampled, watch attention scores form, see what's happening inside the GPU. I found that playing 2:27 with the concepts helps with gaining deeper intuition on them. Yeah, I think the concrete 2:31 examples are important, because when it comes to transformers, there's often more than meets the eye. 2:37 Let's walk through as well what the course actually covers. Okay, let's do it. So in module 2:41 one, you learn about everything an LLM does, whether it's writing code or why it's hallucinating. 2:47 And that all comes from the same process, picking one token at a time from a probability distribution 2:53 of all the possible tokens in its vocabulary. This basically helps explain many LLM behaviors 2:58 that fall out of this process and shows how techniques like structured output or RAG, 3:03 retrieval augmented generation, and reasoning all work by building on that same loop. And that 3:08 raises an obvious question, where does that probability distribution even come from, right? 3:12 That's in module two. So you'll look inside the model there, the attention mechanism, how 3:17 positional information is encoded and included, how layers build on each other to see the math 3:22 that produces it. It's a lot of matrix operations and seeing them helps you understand what the 3:28 model is actually doing with your input. Finally, once you see the math involved in a single forward 3:32 pass, you can begin to understand what goes on in a full LLM system beyond just the language model 3:38 itself. So in module three, you'll look at how that math runs on GPUs, the optimizations like 3:44 KV caching, flash attention, quantization. Again, don't worry if you don't know what those terms are. 3:48 These are techniques that make these larger frontier models fast enough to actually serve 3:53 users. One of the things I really like about this course is that if you're already working with LLMs, 3:58 you probably have a sense of how they behave from your practical experience, 4:03 but not necessarily always why they behave that way. By the end of this course, you have that 4:08 coherent picture that Sharon described that will help you to make better decisions about 4:13 deployments or help you to follow new research and really understand what's happening when things 4:19 go right or when they don't go right. I hope you enjoy learning about how transformers work 4:25 and let's go on to the next video and get started.  |
| :---- | :---- |
| 2 | 0:00Welcome to this course on Transformers in Practice. 0:03The goal of this course is to give you a practical understanding of how transformer models work, 0:08so that when something goes wrong or you need to make a decision about how to deploy a model or configure it, 0:14you have the right mental model to reason through it, rather than just guess. 0:18So let's get started. 0:20This is the architecture behind every large language model out there at the frontier, 0:24just like the ones you're using, like Claude, Gemini, ChatGPT, or some of the open-source ones like Qwen and DeepSeek. 0:30So now there are a lot of different types of transformers, and if you look at the original transformer paper, 0:36you'll see that it's an encoder-decoder architecture. 0:39But the latest generative models that you typically use that are popular today use a decoder-only transformer, 0:46and this has become the dominant architecture, so it's the focus of our course. 0:49You've probably had moments where these models were super useful. 0:52They gave you an incredible answer to a difficult problem, and it walked you through to the solution, and it was all correct. 0:58Everything was all peachy. 1:00But then other times, you get a bad answer, right? 1:04And sometimes it might even feel simple. 1:06Why does that happen? 1:07You get these good results sometimes and bad results other times. 1:10So I'm going to help you build kind of these conceptual understandings of how LLMs work, 1:15and you'll be looking at interactive visualizations, and that'll help make these ideas more concrete throughout the course. 1:21So the first module is really focused on model behavior and really understanding conceptual intuitions of model behavior, 1:28meaning you'll be treating the LLM as a black box, 1:31and you'll be focused on the input and how the output is produced from that input. 1:37In the next module, you'll open up this black box and be able to see some of the representations and processes inside of the architecture, 1:43such as attention or the multi-layer perceptron inside of this model. 1:48Finally, the third module, you'll be scaling and deploying and understanding the full system of this LLM. 1:54There's so much stuff that goes all around it to get you the speed, the cost, the quality that you see and expect from these models. 2:02By the end of this course, you'll have a complete picture of how these models behave, 2:06how the architecture produces that behavior, and what it takes to run them in practice. 2:11We're going to start with what you can observe, how a model generates text one token at a time. 2:17I'll see you there. |
| 3 | 0:00In this video, you're going to look at the transformer as a black box. 0:03Not what's inside it, but what you can observe about its behavior. 0:07You put a prompt in, and text comes out. 0:09So what does that process actually look like from the outside? 0:12Once you see it, you'll start to understand why these models behave the way they do. 0:16Both the things that work well, and the things that go wrong. 0:19When you prompt an LLM, you get a full response. 0:22So sentences or paragraphs as output. 0:25But this response was built one piece at a time, token by token, 0:29through a process called autoregressive generation. 0:32And the specifics of that process impacts the behaviors that you'll see the model generate new text. 0:38So what do I mean when I say an LLM is autoregressive? 0:42It means that this LLM is generating text one token at a time through this loop. 0:47And each new token depends on the previous generated one. 0:50So that's how this loop continually moves around. 0:53And the first thing that happens is your input is first broken into these tokens. 0:58Tokens can be these words, segments, and common words tend to be single tokens. 1:02But the system doesn't only get tokens from your prompt. 1:05It also gets these template tokens. 1:07So the system prompt, so these system tags, 1:10like you are a helpful assistant that describes what the model should act and behave like. 1:14It might also have this tag of /no\_think. 1:17That means not using any thinking tokens, not using any reasoning tokens, 1:21which you'll learn about later. 1:22And then your prompt, “fix this for me,” and your bug. 1:24And you see these LLM end and LLM start tags, these assistant tags. 1:28So there's all these sorts of template tags that the model has learned previously. 1:32And then it knows to understand and how to behave based on this template. 1:36And all these tokens taken together becomes the quote context window. 1:40One important thing to mention is that the model doesn't have static memory 1:44or a memory outside of this context window it's just looking at this context window 1:48with your prompt inside of it and produces one token from this. 1:52And the size of this context window is the maximum amount of text 1:56an LLM can quote unquote see at any point in time. 1:59So you can't go beyond that maximum context window. 2:02So the context window has these tokens. 2:04These are the input tokens into the model. 2:06The model computes probabilities. 2:08You get output probabilities out of the model. 2:11And here's a sample distribution of output probability tokens. 2:15What could be the next token that it predicts? 2:17And you can see here "the" is the most likely token that it could predict next. 2:21So how do you know which next token to select? 2:24The simplest approach is just pick the token with the highest probability. 2:27So here it's "the" and that is greedy selection. 2:30But that's not the only option. 2:31So you could also do random sampling. 2:33So you can randomly pick any of these tokens. 2:35That's called sampling. 2:36And there's a lot of parameters you can use to control what tokens you can choose from. 2:39And you'll learn more about that in another video. 2:42So that's the reason why the same prompt can give you different responses every single time. 2:47Now once you've selected a token, that token is now appended to your input tokens. 2:53And then the whole process goes through again. 2:55It computes the probability distribution again of the next token. 2:58And then here it samples "error". 3:00So then that's appended "is," "due," "to". 3:04And then how do you know when to stop? 3:06So it just keeps going and going and going. 3:08The model has learned these stop tokens that explicitly are used as a stopping condition. 3:13So that you can stop this process from continuing. 3:16And so here the next token sampled is "a". 3:18Is that a stop token? No. 3:20So continue generation. 3:21Let's fast forward a little bit until it actually predicts this token. 3:25And then that stops the generation. 3:27There are other stopping conditions too. 3:29There might be a maximum token limit that you want as output from the model. 3:32And maybe that's 14\. 3:33So as you're sampling here, this is just 13 output tokens. 3:37You can keep going. 3:38But once you reach 14, you have to stop. 3:40Another one is if the context window is of a certain size. 3:44So let's say 41 tokens. 3:45It's really small here. 3:46But it has a fixed size. 3:48Then you also will stop when you have reached that max context window size. 3:52And that's a full loop. 3:53So you have your input. 3:54It gets broken into tokens. 3:55Those tokens are sitting in this context window. 3:58And that's everything the model can see. 3:59The model then computes these probabilities, scores across its entire vocabulary. 4:04And a token then gets selected as that next token. 4:06And it gets added back into the context window. 4:09The model runs this again. 4:10And it repeats until a stopping condition is met. 4:13And that's how every response gets built one token at a time. 4:17So each step of the loop produces a probability distribution across the entire vocabulary of next possible tokens. 4:24But how does a single token actually get picked from all those possibilities? 4:28And how does that choice shape what the model produces? 4:31That's what's next.  |
| 4 | 0:03To give you another opportunity to build intuition about transformer concepts in this course, 0:08you'll have the opportunity to explore a series of interactive visualizations for key 0:13topics. 0:14Let me walk you through some key features of these visualizations. 0:16Okay, so here's the first visualization. 0:18It walks you through the steps of the text generation loop. 0:21It's powered by the Qwen3-0.6B parameter model, which is a cute little LLM that will 0:26run on the Deep Learning AI platform. 0:28And this will let you see real output from an LLM. 0:31So some of the other visualizations are powered by this model, and others will just use pre-calculated 0:35data. 0:36Okay, so when the visualization finishes loading, which might take a minute the first time, 0:40you'll see the screen. 0:41I recommend that you click on the little menu icon next to the course syllabus to collapse 0:44this menu so that the visualization can take up more of the width of your browser. 0:48And you can adjust the size of the visualization using the zoom controls in your browser's 0:51menu. 0:52So to use this autoregressive loop visualization, you can type a prompt in this box or select 0:57a pre-written one with the symbol icons. 0:59Let's pick the speech bubble prompt, which is an example of sentiment analysis. 1:02The prompt is, analyze the sentiment of this review, and then there's an included review. 1:07Once you select the prompt, you can see the input tokens here below. 1:10Gray tokens are the chat template that the Qwen model expects, and the blue prompt is 1:14a system prompt. 1:15If you want to view that, you can click system up above the prompt. 1:18So here's an additional instruction, return exactly one word. 1:22Okay, go back to the prompt view. 1:24Okay, so now you're going to step through this loop using the step button here above 1:28the tokens. 1:29Cool. 1:30So when you click step, this kicks off a forward pass on the model where the entire set of 1:34input tokens on the left is passed through the model. 1:37The output of this forward pass is a set of probabilities across the token vocabulary 1:40for the next token prediction. 1:41You can see the token and the probability here. 1:43In this case, the probability is basically all concentrated in the single token positive, 1:48which is the correct answer. 1:49When you click step again, you'll see the top token being selected and appearing in 1:53this third column. 1:55In this visualization, you're always selecting the most probable token. 1:58This is called greedy sampling or greedy decoding. 2:01And you can sample this distribution in other ways, which you'll learn more about in the 2:04next video. 2:05So you've selected the positive token. 2:08Now let's click step again, and you'll check whether you've met a stop condition of the 2:12loop. 2:13So the loop can actually exit in three different ways that you can see here. 2:16So you can meet your specified max new tokens, or you fill the context window, or you select 2:22a stop token. 2:23So with the positive token, you haven't met the stop condition. 2:26So instead, you can append the token to the input. 2:28And you can now see it over here on the left. 2:31And then you can go through the loop again. 2:33You can do this by clicking step more times, or instead, you can hit play and the steps 2:37will play out automatically. 2:38And you can click faster to speed it up. 2:40This goes through a moment return. 2:41And this time it picks I am end, which is actually a stop token for this model. 2:45This is the end of term token. 2:47And so text generation will stop. 2:49In this and other visualizations, you can click any info buttons that you see, like 2:53this one in the select token section to get more info about what's going on. 2:57This will open up a pop up window with some explanation text. 3:01If you ever encounter any issues with any of these visualizations, look for a reset 3:05button or just refresh the page. 3:07So feel free to play around with this and other viz. 3:09Try the pre-written prompts. 3:10For example, click the music button to see a fun Taylor Swift question, or you can type 3:14out your own. 3:15I hope you find these visualizations useful for understanding the concepts in this course 3:19and for explaining things to colleagues or friends. 3:21However you end up using them, I hope you have fun.  |
| 5 |  |
| 6 | 0:00Last video, you saw the autoregressive loop, which is how the model generates text one token at a time. 0:06You've seen how the model gives you a probability distribution over the entire vocabulary, but you need a single token. 0:12Let's look more closely at the options you have for going from thousands of possibilities to a single choice. 0:17So a quick recap, you have this autoregressive loop where you have input tokens and you compute probabilities, 0:24you get these next token probabilities out, you sample one of them, and then you do that over and over again in the loop 0:30until you meet some stopping criteria. 0:32Now, the focus of this video will be on that selection of that token. 0:36How are you going to select it? 0:37Maybe it was greedy, then most likely token, but there are other ways of sampling across this distribution. 0:42So using greedy sampling, you pick the highest probability next token. 0:47And this full process of decoding using the language model here is called greedy decoding. 0:52And this is almost deterministic, right? 0:54You're picking the highest probability token every single time. 0:57But why is this not always a good idea? 0:59Well, as a result, the language models sometimes sound a bit boring and a bit boilerplate when you pick the most likely token every single time. 1:06And that's not always what you want. 1:08And historically, sometimes the model will get into these repetitive loops when you do greedy decoding. 1:14So random sampling will fix some of these problems because you don't always select the most likely token. 1:19So you're selecting more randomly. 1:20It's kind of the spin the wheel here where you can spin this wheel. 1:23It's weighted by these probabilities and it's a little bit random. 1:26There's a little bit of chance here of what token you will land on next. 1:31Now, you do have to be careful with random sampling, right? 1:33If you sample from all possible tokens, including super low probability ones, 1:37you might pull up and select a rare token that makes absolutely no sense as the next token. 1:41So there are a few different parameters you can play with when you're sampling that next token with random sampling. 1:47We'll start with this one called temperature. 1:48Temperature is about changing these probabilities and changing this distribution so that you're more likely or less likely to pick certain tokens as the next token. 1:57Specifically, it draws from physics. 1:59It is the same temperature as you see in physics as warmth or coldness. 2:03When temperature is larger, you have a bigger spread across the possible next token probabilities. 2:09And this makes it so it's less likely to pick that highest probability token and spreads it out across the distribution. 2:16Now, when temperature is very low and when you set it to zero, it's going to be similar to greedy sampling where you pick only the most likely token there. 2:24Another parameter you can play with is straightforward. 2:26It's called top K. 2:27And what it means is you're just picking the top K tokens and you're picking out of those top K. 2:32So if top K is one, you're still picking that top next token probability. 2:35Top K is two. 2:36You're picking out of these two highest probability tokens and so on and so forth. 2:40So for five, you'll pick top five. 2:42Now, that sounds all fine and good, but it's not always optimal. 2:45So if you look at these two possible prompts that you could give to the model and you set top K to five for the one on the left, maybe that's fine. 2:52All these possible next tokens are all fairly likely and decent possible responses and paths for the model to start off on. 2:59But if you look at the right, you'll see that the first two are very likely tokens to be sampled. 3:06But the next ones are very low probability. 3:09So low, like 0.2%, 0.1%, and so low that it might veer the model off into a really weird space. 3:16So maybe that's not optimal here. 3:18So here's another parameter, top P, where P stands for probability. 3:22And instead here, you can provide a percentage from zero to 100%. 3:26If you use zero, it'll be greedy decoding. 3:28But 100% selects cumulative probability of 100% of all the tokens. 3:33So if you set it to 99% here, it's the cumulative probability of 99% of the probability mass and then some. 3:41So here you can see it's 99.1% is all of these tokens adds up to that. 3:46If you bring it down top P to 90%, it'll get a probability mass covering that 90% and then some for that last token. 3:54So that's 94.7%. 3:56And if you take it all the way down to 80%, so the total cumulative probability will have to reach and cover that 80% probability mass. 4:05You'll select from fewer tokens for that next token probability. 4:08So how does this play with temperature as a parameter? 4:11Well, if you have a different temperature, but you keep the same top P, you can see how your distribution might shift. 4:18So with a lower temperature, because you've sharpened the distribution. 4:21Now, a top P of 90% only keeps a few of those tokens there. 4:25Whereas if your temperature is higher, you've spread that distribution out across your different possible next tokens and your top P is still that 90%. 4:32It captures more of those possible tokens. 4:35So you play with these together. 4:37And realistically, these frontier models do play with them together under the hood when you're using them. 4:40But providers don't always expose all of these parameters. 4:43In fact, OpenAI doesn't expose top K and suggests changing either temperature or top P, but not both for you as the end user. 4:50So in summary, there are a bunch of different sampling strategies. 4:53You can use greedy decoding, you can play with temperature, and you can also play with top K and top P. 4:59You'll have a chance to explore the sampling process in a visualization following this video. 5:04So now you know how the loop works and how tokens get selected from the distribution. 5:08The key thing to keep in mind is that the model is always selecting based on probabilities, not correctness. 5:14So it's probabilistic based on patterns learned in its training. 5:17This means a lot of interesting model behaviors follow from exactly those properties. 5:22And that's what you'll explore in the next video.  |
| 7 |  |
| 8 | 0:01Let's quickly revisit the process you've been building up over the last two videos. 0:04So, just to recap, you have your model with input tokens, it computes probabilities, you select the next token based on these sampling parameters, 0:11and you do that in a loop until you meet some stopping criteria. 0:15But what actually happens as a result? What kinds of behaviors do you see when you use these models, and why do they happen? 0:22That's what this video is about. 0:24And this process of autoregressive generation can lead to many different paths. 0:28So, for example, here you might ask the model to write a Python function to validate user email addresses, 0:33and it might start sampling the token def to start defining that Python function. 0:37But it could also predict and sample that next token as import, and so it's just importing that regex package, 0:44it's still writing that valid Python function and accomplish that task, but it's going to look different. 0:49However, a more problematic scenario can happen if the first token that it selects is function, right? 0:55So that is not how you would start a Python function, that is how you would start a JavaScript function. 1:00And so in this case, it starts writing a JavaScript function, it can't backtrack. 1:04So this behavior you might also see in math, because numbers are generated one digit at a time, 1:09and this is often why people use a calculator tool for language models instead, 1:13because the language model might do some things here that are problematic. 1:17And so far, it's so good, but then it reaches the wrong digit. 1:21So at first, it was fine, and then it reaches this wrong digit of zero. 1:24And then it just keeps going, it can't backtrack. 1:27Another bad possible choice is you're asking if this number is a prime number, and the correct answer is yes. 1:33But if it happens to sample no first, then everything subsequent after that will be wrong, 1:38and it'll potentially keep fabricating facts. 1:42So as you can see, picking a bad token can lead to all sorts of problems, right? 1:47Once you reach that bad token, it can lead to the model hallucinating or making things up confidently, 1:51it can lead to that task drift that you saw where you asked for a Python function, but it produced a JavaScript function. 1:57It can also lead to other problems like repetition. 2:01And a very famous example here is where you could ask the model how to upload a model to Hugging Face, 2:06and the model would suggest a fake package because people were asking this model how to upload a model talking face. 2:12A security researcher created a fake PyPy package for a hugging face CLI, 2:16and it resulted in 30,000 downloads in just three months. 2:20Models will also often default to the most frequent patterns that it's learned from training. 2:24So if you're converting from Celsius to Fahrenheit, here's the equation of multiply by 9, divide by 5, and add 32\. 2:30The model does quite well on this. 2:32But if you perturb it slightly, the model might still default to doing that temperature conversion, 2:37and this is what some researchers found, because it is so used to doing that, because that was just much more likely. 2:43Another type of hallucination might be just lack of knowledge. 2:46The model might not know about this package that you're asking about, 2:50and it might just start making it up because it wants to give something probable instead of saying it doesn't know. 2:56And of course, the model can also enter into this almost self-reinforcing loop 3:00where it repeatedly generates the same sequence of tokens, resulting in extreme repetition. 3:04And this tends to happen more with those older models or smaller models. 3:08Autoregressive dynamics can also lead to many paths, as you saw. 3:11It can branch out to many correct paths, but also many incorrect paths. 3:15So ultimately, language models don't necessarily see truth. 3:18They see probability, and the most likely token might not be the most correct token. 3:22In the visualization after this video, you'll get to see the impact of different token choices 3:26on everything that's generated afterwards, and get a feel for how likely a given response is. 3:31However, researchers have been trying to tackle this problem in post-training, 3:34so making sure the model is more likely to select that correct token, 3:38and making the most likely token the most correct one. 3:41So now you've seen the autoregressive loop, how tokens get selected, 3:44and the kinds of behaviors that follow from that process, for better or for worse. 3:49So now that you understand these behaviors, you can start influencing them. 3:53In the rest of this module, you'll see how. 3:55Let's start with structured outputs.  |
| 9 |  |
| 10 | 0:02So you've seen how the autoregressive loop works and the kinds of behaviors that fall out of it. 0:07Starting with this video, you're going to look at ways to work with that process to get better results. 0:12So as you saw in the previous video, you can't strictly just force the model to provide a specific answer. 0:17There's a bunch of probable tokens that it could select. 0:20You can use different techniques to sample that next token, 0:23but there will always be many different possibilities due to the probability distribution. 0:27But what happens when you have to follow a certain structure? 0:29How do you make sure the output conforms to a format without changing the model itself? 0:33When do you need that structure? 0:34One, JSON output. 0:36You just want structured data extraction as output, 0:39and then you're going to use that output from the model for some downstream task. 0:43And so you want it to be very consistent in producing that JSON output. 0:46Code generation, you want to follow a certain schema, certain grammatical rules. 0:50Function calling, it needs to generate a valid API with correct parameters. 0:54SQL generation as well, it needs to match what is in the database. 0:58So one of the things that is difficult for the model when it's training, 1:02it wants to balance being structured but also being helpful and saying things like, 1:06sure, I can help you with that. 1:07And that results in potentially inconsistent outputs. 1:11For example, here you can see the next token probabilities from the model. 1:14Some are helpful, so it can say sure or here, 1:17but it could also produce these brackets and braces to produce structure. 1:22And so maybe you want this JSON output and you do put an email through 1:26and the LLM is able to give you that structured JSON output, 1:29and it's able to do it over and over again until it doesn't. 1:33And then it wants to be helpful. 1:34It says sure, here's the JSON output for the email you provided 1:37instead of just going directly and giving you that JSON. 1:40So this is difficult because now you have to do some post-processing afterwards 1:43and remove that. 1:44So here it is also giving you JSON tags before giving you that JSON output. 1:48Okay, so this results in a ton of post-processing that you might have to do. 1:52So how do you constrain the model to always produce that structured output for you? 1:57So this is called constrained decoding. 1:59Here's an example. 2:00So this is for JSON. 2:02Is this review positive? 2:03The battery died after a week and reply with JSON. 2:05And what you want to do is you give the model a schema. 2:08And specifically what's in that schema here is output that this JSON starts 2:13and ends with curly braces and the fields that are mandatory. 2:17So there are only certain valid fields, and this is the answer field. 2:20The answer can only be a string and can only be the yes or no string. 2:23And it's required. 2:24It has to show up. 2:25So this is what a schema might look like. 2:27Because the schema has a structure, the decoder needs a way to keep track 2:31of where it is in that structure. 2:33For example, has it started the JSON object? 2:35Is it inside a key name? 2:37Is it after a colon where the value should come next? 2:39That's where a finite state machine, or FSM, comes in. 2:43An FSM is a way to track where you are in a process using a fixed 2:47or finite set of possible states. 2:49In constrained generation, the key idea is that the FSM tracks the current 2:53position in the required structure. 2:55After each token is generated, it moves to the next state. 2:58And that state tells the decoder which tokens are valid next. 3:02And you mask the next token probabilities. 3:05So some of the tokens are valid, some are not. 3:07You want to mask the ones that are not. 3:09And you only allow the model to sample from the ones that are valid. 3:12And as you sample, it has to sample this open curly brace first, right? 3:16So that's the only possible one. 3:17And then the next token, there's a few valid ones that are not masked. 3:21Out of those, it selects the open quote. 3:24And then it keeps going. 3:25And then it selects, selects, selects, until you get this response. 3:29And it is valid JSON. 3:31It follows that schema. 3:32Don't worry if the finite state machine concept here feels mysterious. 3:35You'll get to see it in action, step by step, 3:37in the visualization following this video. 3:39So how much does this improve the language model? 3:41Well, quite a bit. 3:42This is from a paper that was studying this. 3:45It could get 40% success on GPT-4o in 2024 without constraints. 3:50But you could get 100% with constraints, right? 3:52This is not all sunshine and rainbows. 3:54So it's able to adhere to the schema, follow the right syntax. 3:57But that does come at a cost of accuracy. 4:00So here, you're giving the model this word problem. 4:02And the language model produces this answer that is correct 4:06if it doesn't use constrained decoding, but then incorrect if it's constrained. 4:09So the model might conform to your schema appropriately, 4:12but it might be more likely to produce the wrong answer, ultimately. 4:15Constrained decoding is available on the popular APIs. 4:18For example, on OpenAI, you just pass a Pydantic model 4:21or a raw JSON schema into the response format, 4:23and you get the response to conform to that structured output. 4:26Same with Anthropic. 4:27You can put a JSON schema or a Pydantic model as well. 4:30And for the self-hosted models, there are different inference engines 4:33that use underlying libraries to help you with constrained decoding. 4:37In the lab for this module, you'll get hands-on practice 4:40comparing a prompt-only approach with constrained decoding. 4:43So you can actually see the difference between asking for structured output 4:46and enforcing it. 4:48So you can control the format of the model's output. Great. 4:51But what about what it actually says? 4:54The model can only draw on patterns from its training data. 4:58If you ask it about your private data 5:00or something that happened after it was trained, 5:02those tokens simply aren't going to be the most probable ones. 5:05So how do you get a model to produce results 5:08grounded in information it was never trained on? 5:11That's what's next. |
| 11 |  |
| 12 | 0:00You've seen how to control the format of the model's output, but what about the content itself? 0:04There are ways to enforce structure like you saw, however that doesn't solve the problem of being 0:08more accurate. The model can only draw on what it learned in training. If the answer you need 0:14isn't represented well in that training data, the model is going to output whatever is most 0:18probable, which might be wrong. So the model is trained on many different sources of data, 0:24and this knowledge is frozen at the date of training, and that means if there's data 0:28generated after training, it won't know about it. If there's internal documents in your company, 0:33it won't know about it, and if you have personal data, it won't know about it. 0:36So here are three prompts where it would fail automatically. So one is it doesn't have internal 0:41information from Acme Corp. It was trained before this event happened, the 2026 FIFA Women's 0:47Champion Cup, then it wouldn't know, and maybe it didn't have access to this information about 0:52these drugs during training, and so these would all result in these failed responses. And in this 0:58case, the model wasn't trained to have a I don't know token, so it's inventing things that sound 1:02right. So who won the 2026 FIFA Women's Champion Cup? The model is still going to produce tokens 1:08that are highly probable, and it'll produce this wrong answer of France. But if you give it context 1:14on this latest information, maybe you use a bit of search, and you got this context, and you add 1:19that to your prompt, then it's more likely to produce the right answer. Here that would be 1:23Arsenal won the 2026 FIFA Women's Champion Cup. One thing you can do as a user is inject context 1:29manually, and you probably have done this while you code. You attach lines of code or files where 1:33you want to debug something, and the language model can then use that as context and then fix 1:38it for you. So manually injecting context is all fine and good, but sometimes when that code that 1:43you want to add to the context is too large, or there's too many documents you want to add there, 1:47or the documents keep changing, so it doesn't necessarily make sense to do it in this way. 1:52The solution is a set of tools called grounding, kind of grounding the model at the input context 1:57layer with information that's useful and that you can do so automatically. One very popular way is 2:03called retrieval augmented generation or RAG, which you're probably familiar with, but essentially 2:07grabbing relevant document chunks that are stored in a database that are similar to the prompt that 2:11you're giving. Of course, you can do search as well, so that's just doing live search to get 2:15relevant information that's up to date on the web. Tool use, so making it possible for the model to 2:19call different tools and query different APIs to get information. Search is one possible tool, 2:24one very popular tool. And finally, you can use things like structured information from knowledge 2:28graphs that can ground the model in useful entities and relationships, in this case about linked facts. 2:35Going a bit into RAG here, since it's a dominant grounding method, what the pipeline looks like 2:39is you can chunk and embed a lot of documents. So you have a ton of documents in your database, 2:45and you chunk them, you embed them so that they're stored somewhere in a vector database. 2:49A user submits a query, and that query is then searched against that vector database 2:54of previously chunked and embedded documents. And then you might select the top K most semantically 2:59similar chunks, and those are then retrieved. And K could be three here, so you pick three of the 3:03most similar chunks, and you concatenate those into the context window of the LLM. And then the LLM 3:08is able to make a better response condition on both the query that you put in and also the 3:13retrieved context. So does RAG improve the model's accuracy? Yes. And there was a systematic 3:18review covering papers from 2020 to May 2025 that confirmed that RAG did help reduce hallucinations 3:24consistently and especially outdated knowledge across a diversity of tasks. But RAG is not all 3:29sunshine and rainbows. It's really just injecting extra strings inside of your context window. 3:33And so what happens is when you ask a question and RAG retrieves a bad document or contradictory 3:39document or chunks that aren't properly chunked up, it results in contradiction or something 3:44incorrect or seemingly incorrect, then the language model will use that incorrect information 3:49to produce a bad result, a hallucination. So here's an example of contradictory documents. 3:54So you might ask, how do I authenticate requests to our internal API? And maybe the documents 3:59retrieved include this one where it was last modified V1 of your API docs, last modified in 4:042021, and then a second one, the V2, last modified in 2023\. So updated docs, right? Very common. 4:11But now your model retrieved both of them. It says both formats are supported. That's not true. 4:16So this is an example of where contradictory documents in RAG that were retrieved can result 4:22in this hallucination. Another example with poorly chunked documents is this. So you might have this 4:28SDK errors file and a list of different things in there. And it's very long, right? So you have 4:33to chunk it up. And as a result, the way you're chunking it, maybe it was naive, you do a chunk 4:37here, half-half. Okay. One issue is that the first chunk includes a certain amount of information, 4:43whereas the second chunk is saying like, oh, actually, you can recover from that permanent 4:48ban of your API key by waiting at least 30 seconds before retrying. So now you'll see in here, when 4:54you ask, how do I handle a rate limit error in the SDK, the first chunk is selected because it just 4:59seems more semantically similar. And chunk B was not selected, even though it showed how to 5:04recover from that error. And as a result, the model thinks you can't recover from that error, 5:09right? It just says it'll result in a permanent ban. So it has incomplete knowledge about that 5:13file, incomplete context. And this was as a result of how you were chunking and fragmenting 5:18your documents. Another problem that researchers have observed is that models have to pay attention 5:24somewhere in the context, and they tend to pay attention to the beginning and end of the context 5:28window. And this is an observation called the loss-in-the-middle phenomenon. But essentially, 5:31the amount of attention that it puts on the beginning and end are places where the model 5:35is paying attention. But if you put something in the middle that you really want the model to pay 5:40attention to, it might miss it. So overall, to handle all of this is an area called context 5:46engineering, where engineers are devising ways to make sure the model gets what it needs to produce 5:52an accurate result, where maybe you have a few good documents instead of many mixed good and bad 5:57documents. So you're engineering your context. You're making sure that the way you're chunking 6:01things is semantically or structurally aware. And then you're putting documents where they get the 6:06most attention, maybe at the beginning and end, especially the important ones that you know are 6:10really important when you're retrieving things. RAG, of course, is not a definite solution. It's 6:14not a hard enforcement of the model. RAG is a soft conditioning. And the model can ignore that 6:20retrieved context, for example, if you ask what is the newest model by OpenAI. But from its training, 6:25it was checkpointed back further, and it thinks previously it was GPT 3.5 Turbo. The model is 6:31weighing these possible probabilities of the grounding info and what it was trained on. And so 6:35sometimes it might still go back to the training information. Ultimately, the goal is to make RAG 6:40more reliable, but it is not, again, fully error free. It's just the type of engineering to help 6:44with that. And in different areas, specifically like medical, legal, financial, there's additional 6:49validation steps that people put in place in addition to RAG to make sure that these results 6:54are not wrong. In the lab for this module, you'll get hands-on practice building a full 6:58RAG pipeline. So you've seen that what's in the prompt directly shapes what the model produces. 7:04The right context makes the right tokens more probable. But what if the model could generate 7:09useful context for itself? What if its own intermediate output created by working through 7:14a problem step by step could improve the answer that comes after? That's what you'll look at in 7:18this next video.  |
| 13 | 0:01So you've been impacting the model's behavior at different points in the loop. 0:05Grounding works at the input stage, and sampling parameters like temperature, top P, top K, as well as constrained decoding, 0:12they work at the probability distribution computation where you select that next token. 0:17But there's one more idea, and it comes from the loop itself. 0:20Remember, every token the model generates becomes part of the input for the next token. 0:25So what if the model uses that to its advantage, generating intermediate steps that help it get to a better answer? 0:32So as you know, every token that goes out of the model actually becomes part of the input for the next iteration, the loop for that next token. 0:38So how can you use that to your advantage? 0:41So there are types of problems that benefit from exactly that, and these are problems that require what we call reasoning. 0:46So, for example, you have this math problem, and without reasoning, without the model being enabled to think through the problem step by step, 0:54it might just guess what the answer is, like there are five apples, and that is wrong. 0:58Another type of problem that might need real reasoning is debugging anything, right? 1:03The model might benefit from being able to iterate on possible results instead of guessing at it all at once. 1:09So reasoning is something that the model can do at this intermediate step of the loop. 1:14Let's take a look at exactly what that looks like. 1:16So how does reasoning work? 1:19So instead of answering right away, like there are five apples here, and if you force the model to give you a short answer right away, it might be completely off. 1:27You give it space to think step by step and break the problem down. 1:30And one of the first discoveries of this was the idea of just asking the model on the prompt to think step by step. 1:35And then it thinks step by step, so it starts with five apples, gives two to Sarah, etc. 1:39It breaks down this math problem and is able to give you a final answer of two apples that was correct. 1:45Broadly, this is called reasoning. 1:47That's the technical term. 1:48The model is giving different steps, so producing a chain of different thoughts that are often invisible to the user that you might not have to expose. 1:55And it's often enclosed in what we see as think tags. 1:59It can also be called, quote, thinking as a technical term. 2:02Of course, the model might not actually be thinking. 2:03It's just called thinking and then produces a final result. 2:06And in this case, it says there are two apples left and that would be exposed to the user. 2:11For example, here is on Claude. 2:13You might see this gray text here. 2:15That is that thinking, quote, process hidden by default. 2:19You could click into it and see those step by step things happening. 2:22But ultimately, you see that final result and that's exposed to the user. 2:26But beyond just regular chain of thought, the model could also try something, autocorrect itself, reflect on it and realize, oh, that's not right. 2:34Let me try something else. 2:35So, for example, here you can trace your function. 2:37Oh, the bug isn't in that logic. 2:39It's in that window. 2:40Oh, but actually this algorithm is a well-known solution and it's correct. 2:43So it will go through all these possibilities before it reaches a final result. 2:48Not unlike a person thinking through a problem rather than immediately getting to a result. 2:53However, it's not perfect. 2:55So the model could fall into a wrong reasoning step. 2:58So in this example, the model decides that the code is some standard implementation. 3:02It'll lead to a different result. 3:03And if the model is unable to correct itself through this process, it'll follow a totally different chain of thought. 3:09And maybe conclude that the problem is not the code and the user has an outdated Python version and results in something wrong. 3:15So it doesn't fix everything, but it is very useful to help the model correct itself. 3:19So how does reasoning really operate in a multi-term scenario? 3:22And how is that added to the context window? 3:24So remember broadly that in your context window where you ask a question, you have a query to the model. 3:29The model produces an answer that gets appended to the context window. 3:33And then you ask another question and produces an answer, et cetera, et cetera. 3:36Now specifically with reasoning, there's that thinking area, right, with the think tags. 3:41And that's produced as output. 3:42And you could theoretically just keep appending that through. 3:45But that would drive your context window to fill up very, very quickly. 3:48So often what happens is then the context window for thinking versus without the thinking turned on would be just much, much larger and run out of space much more quickly. 3:58And as a result, you wouldn't be able to interact with the model for a longer session. 4:02So a practical way of dealing with this is typically that thinking area thrown away every single turn and not appended to the context window. 4:10So you're still using that. 4:12You're still giving that scratch space for the model to, quote, think. 4:15But then it's not appended to that context window. 4:17So only that answer is appended to that context window. 4:20You ask the next question, et cetera, et cetera. 4:22It's still using that space to think through things step by step, use chain of thought. 4:27But that is thrown out. 4:28And so this is how Claude handles reasoning in its conversations. 4:32However, it's worth noting that in some of the latest models, some providers do keep a summary, at least, of the model's thinking process instead of throwing it all out because there's some richness in there where the model had tried something before. 4:44And so that prevents the model from trying that wrong solution again. 4:48All right. 4:49So you've reached the end of Module 1\. 4:50In this module, what have you seen? 4:52You've seen the LLMs work as an autoregressive loop. 4:55This autoregressive behavior is not totally predictable, right? 4:58It can lead to a lot of different paths, some of them right, some of them wrong. 5:01There's a set of strategies that you can use to improve the model's behavior at different stages. 5:06So grounding works at input. 5:07The sampling parameters and constrained decoding work on those computed probabilities and enabling to select the right token or select a better token. 5:14Reasoning works by generating intermediate results that improve the model's context and make it give better answers. 5:20And all these methods have their caveats. 5:22So use them with care and use them together. 5:24So everything you've seen in this module from the autoregressive loop to sampling, hallucinations, grounding, reasoning, all of it comes from the same process. 5:33The model outputting a probability distribution and selecting a token one step at a time. 5:39You've been working with that process from the outside, observing it, influencing it, improving it. 5:44But you haven't looked at how that probability distribution actually gets produced. 5:49What's happening inside the model that turns your input into those probabilities? 5:53That's what Module 2 is all about. 5:56And I'll see you there. |
| 14 | **Module 1 — Additional Reading** The lecture notes are available on our community platform. If you’re already a member, log in to your account and access the [lecture notes](https://community.deeplearning.ai/t/transformers-in-practice-lecture-notes/892137). NOTE: If you don’t have an account yet, kindly follow the [instructions](https://community.deeplearning.ai/invites/2nAYYz4mZp?_gl=1*1qbil1a*_gcl_au*MTczMjU1NDUzMy4xNzcwOTM3NTE2*_ga*NDM3Mzc5MjI2LjE3NTQ0OTE2MDU.*_ga_FR2MZ1VLMS*czE3Nzg1MTM1MjUkbzQ2MCRnMSR0MTc3ODUxNTA5NSRqMTEkbDAkaDA.), then come back to this page. Here is a curated list of resources that go deeper into the topics covered in the videos. You don't need to read everything. Pick what looks most interesting or most relevant to the parts of the assignment you want to understand better. **1\. The Autoregressive Loop & How LLMs Generate Text** Foundational reads on how next-token prediction actually works. The Illustrated GPT-2 (Visualizing Transformer Language Models) — Jay AlammarA visual walk-through of how a decoder-only transformer produces one token at a time. [https://jalammar.github.io/illustrated-gpt2/](https://jalammar.github.io/illustrated-gpt2/) How GPT models work — Hugging Face LLM Course, chapter on causal language modelingExplanation of the autoregressive loop with code examples. [https://huggingface.co/learn/llm-course/chapter1/6](https://huggingface.co/learn/llm-course/chapter1/6) Hugging Face transformers — Text generation strategiesOfficial docs covering greedy sampling, beam search, stopping criteria, max\_new\_tokens, EOS tokens, etc. Directly relevant to the generate helper used in the lab. [https://huggingface.co/docs/transformers/main/en/generation\_strategies](https://huggingface.co/docs/transformers/main/en/generation_strategies) GenerationConfig referenceAll the knobs (temperature, top\_k, top\_p, do\_sample, eos\_token\_id, …) you can pass to model.generate(...). [https://huggingface.co/docs/transformers/main/en/main\_classes/text\_generation](https://huggingface.co/docs/transformers/main/en/main_classes/text_generation)  **2\. Tokenization** Useful if you want to understand why "the same words" can chunk into very different tokens. Let's build the GPT Tokenizer — Andrej Karpathy (video \+ repo)Builds a BPE tokenizer from scratch. Great mental model for what the tokenizer in the lab is doing. [https://www.youtube.com/watch?v=zduSFxRajkE](https://www.youtube.com/watch?v=zduSFxRajkE) [https://github.com/karpathy/minbpe](https://github.com/karpathy/minbpe) Tiktokenizer — interactive tokenizer playground for many models (GPT-4, Llama, Qwen, …)Paste any text and see the tokens. Very handy when debugging odd model outputs. [https://tiktokenizer.vercel.app/](https://tiktokenizer.vercel.app/)  **3\. Sampling: temperature, top-k, top-p** The Curious Case of Neural Text Degeneration (Holtzman et al., 2019\)The paper that introduced nucleus (top-p) sampling and explained why pure greedy / pure random both fail. [https://arxiv.org/abs/1904.09751](https://arxiv.org/abs/1904.09751) How to generate text: using different decoding methods for language generation with Transformers — Hugging Face blogPractical comparison of greedy, beam, top-k, and top-p with code. [https://huggingface.co/blog/how-to-generate](https://huggingface.co/blog/how-to-generate)  **4\. Hallucinations & Failure Modes** Survey of Hallucination in Natural Language Generation (Ji et al.)A broad academic survey of why models hallucinate and how it's measured. [https://arxiv.org/abs/2202.03629](https://arxiv.org/abs/2202.03629)  **5\. Structured Output & Constrained Decoding (Part 1 of the lab)** Outlines documentationThe library you use in Exercise 2\. Start with the output types guide and the JSON schemas section. [https://dottxt-ai.github.io/outlines/](https://dottxt-ai.github.io/outlines/) JSON schemas: [https://dottxt-ai.github.io/outlines/latest/features/core/output\_types/\#json-schemas](https://dottxt-ai.github.io/outlines/latest/features/core/output_types/#json-schemas) OpenAI — Introducing Structured Outputs in the APIBackground on the schema-adherence jump discussed in the video. [https://openai.com/index/introducing-structured-outputs-in-the-api/](https://openai.com/index/introducing-structured-outputs-in-the-api/) "Let Me Speak Freely? A Study on the Impact of Format Restrictions on Performance of Large Language Models" (Tam et al., 2024\)The paper showing that over-constraining the model's reasoning space can hurt accuracy. Important nuance to know before you reach for constrained decoding everywhere. [https://arxiv.org/abs/2408.02442](https://arxiv.org/abs/2408.02442) Pydantic documentationYou use BaseModel and Literal types to define the target schema. Skim the "Models" and "Field types" pages. [https://pydantic.dev/docs/validation/latest/concepts/models/](https://pydantic.dev/docs/validation/latest/concepts/models/) Other structured-output libraries worth knowing about Instructor (Pydantic \+ OpenAI/Anthropic): [https://python.useinstructor.com/](https://python.useinstructor.com/) LM Format Enforcer: [https://github.com/noamgat/lm-format-enforcer](https://github.com/noamgat/lm-format-enforcer) JSON Schema reference (used by Pydantic under the hood): [https://json-schema.org/learn/getting-started-step-by-step](https://json-schema.org/learn/getting-started-step-by-step)  **6\. Retrieval-Augmented Generation (Part 2 of the lab)** Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks (Lewis et al., 2020\)The original RAG paper. [https://arxiv.org/abs/2005.11401](https://arxiv.org/abs/2005.11401) "Lost in the Middle: How Language Models Use Long Contexts" (Liu et al., 2023\)Why dumping huge amounts of context (the "flooded" condition in Exercise 5\) can actually hurt. Required reading if you want to understand your stress-test results. [https://arxiv.org/abs/2307.03172](https://arxiv.org/abs/2307.03172) Sentence Transformers documentationThe embedding model all-MiniLM-L6-v2 used in the lab is a Sentence Transformer. [https://www.sbert.net/](https://www.sbert.net/) Model card: [https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2](https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2) MTEB — Massive Text Embedding BenchmarkLeaderboard for embedding models. Useful when you need to pick something better than MiniLM for production. [https://huggingface.co/spaces/mteb/leaderboard](https://huggingface.co/spaces/mteb/leaderboard) Chunking strategies for LLM applications — Pinecone learning centerPractical overview of fixed-size, recursive, and semantic chunking. Relevant to Exercise 3\. [https://www.pinecone.io/learn/chunking-strategies/](https://www.pinecone.io/learn/chunking-strategies/) LangChain — text splitters referenceIf you want to see how a production framework handles paragraph- and structure-aware chunking (RecursiveCharacterTextSplitter, MarkdownHeaderTextSplitter, etc.). [https://reference.langchain.com/python/langchain-text-splitters/](https://reference.langchain.com/python/langchain-text-splitters/)  **7\. Reasoning & Chain-of-Thought** Chain-of-Thought Prompting Elicits Reasoning in Large Language Models (Wei et al., 2022\)The paper that started the CoT wave. [https://arxiv.org/abs/2201.11903](https://arxiv.org/abs/2201.11903) DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning (2025)A modern, openly-described reasoning model that uses \<think\> traces exactly like the videos describe. [https://arxiv.org/abs/2501.12948](https://arxiv.org/abs/2501.12948) Anthropic — "Visible extended thinking" docsHow Claude exposes (and hides) reasoning traces in practice. [https://platform.claude.com/docs/en/docs/build-with-claude/extended-thinking](https://platform.claude.com/docs/en/docs/build-with-claude/extended-thinking)  **8\. Tools & Datasets Used in the Assignment** Qwen2.5-1.5B-Instruct — model card and usage[https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct](https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct) lewtun/github-issues — the GitHub issues dataset used in Part 1[https://huggingface.co/datasets/lewtun/github-issues](https://huggingface.co/datasets/lewtun/github-issues) PyTorch documentation — the corpus used in Part 2[https://pytorch.org/docs/stable/](https://pytorch.org/docs/stable/) Hugging Face datasets libraryHow load\_dataset, filter, and shuffle work. [https://huggingface.co/docs/datasets/](https://huggingface.co/docs/datasets/)   |
| 15 |  |
| 16 |  |
| 17 |  |

